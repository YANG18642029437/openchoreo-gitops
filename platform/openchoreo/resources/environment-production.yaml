apiVersion: openchoreo.dev/v1alpha1
kind: Environment
metadata:
  name: production
  namespace: default
  annotations:
    openchoreo.dev/display-name: Production
    openchoreo.dev/description: Production environment; promotion requires an explicit release operation
  labels:
    openchoreo.dev/name: production
spec:
  dataPlaneRef:
    kind: ClusterDataPlane
    name: default
  isProduction: true
