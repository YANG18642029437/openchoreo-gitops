apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: argocd-tls
  namespace: argocd
spec:
  secretName: argocd-tls
  issuerRef:
    name: homelab-root-ca
    kind: ClusterIssuer
  dnsNames:
    - argocd.openchoreo.home.arpa
